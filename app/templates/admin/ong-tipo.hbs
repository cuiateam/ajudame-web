<div class="container">    
    {{#link-to "admin.ong-tipo.new" tagName="a" class="btn btn-primary pull-right"}}Novo{{/link-to}}
    <h2>Tipos de ONG</h2>
    <div class="container">        
        {{outlet}}
    </div>
    <div class="container">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nome</th>
                </tr>
            </thead>
            <tbody>
                {{#each model as |item|}}
                    <tr>
                        <td>{{item.id}}</td>
                        <td>{{item.nome}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>