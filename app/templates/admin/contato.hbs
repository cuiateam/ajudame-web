<div class="container">    
    <h2>Contato</h2>
    <div class="container">        
        {{outlet}}
    </div>
    <div class="container">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nome</th>
                    <th scope="col">E-mail</th>
                    <th scope="col">Mensagem</th>
                </tr>
            </thead>
            <tbody>
                {{#each model as |item|}}
                    <tr>
                        <td>{{item.id}}</td>
                        <td>{{item.nome}}</td>
                        <td>{{item.email}}</td>
                        <td>{{item.mensagem}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>