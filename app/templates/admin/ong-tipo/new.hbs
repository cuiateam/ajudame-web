<div class="col-md-8 col-md-offset-2">
    <div class="well">
        <form>
            <div class="form-group">
                <label for="Nome">Nome</label>
                {{input type="text" value=model.nome class="form-control" placeholder="Tipo"}}
            </div>
            <div class="form-group">
                <button type="subimt" class="btn btn-primary" {{action 'saveOngTipo' model}} disabled={{unless model.isValid true}}>Salvar</button>
            </div>
        </form>
    </div>
</div>